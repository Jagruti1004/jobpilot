import { Typography, Empty } from 'antd';

export const ApplicationBoardPage = () => (
  <div>
    <Typography.Title level={3}>Applications</Typography.Title>
    <Empty description="Kanban board coming in Phase 3" />
  </div>
);