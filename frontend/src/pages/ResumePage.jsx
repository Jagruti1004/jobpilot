import { Typography, Empty } from 'antd';

export const ResumePage = () => (
  <div>
    <Typography.Title level={3}>Resume</Typography.Title>
    <Empty description="Resume upload coming in Phase 2" />
  </div>
);